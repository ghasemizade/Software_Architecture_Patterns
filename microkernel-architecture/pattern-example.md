# نمونه الگو



```
از دیدگاه فناوری، به معنای واقعی کلمه ده ها راه وجود دارد
این ماژول ها قابل پیاده سازی هستند. به عنوان مثال، در جاوا پلات-
از این رو، صفحه نمایش مشتری می تواند یک صفحه نمایش چهره های سرور جاوا (JSF) باشد
همراه با نماینده مشتری به عنوان ترکیب لوبیا مدیریت شده
ننت شی مشتری در لایه کسب و کار می تواند یک Spring محلی باشد
لوبیا یا یک دانه EJB3 از راه دور. اشیاء دسترسی به داده نشان داده شده در
مثال قبلی را می توان به صورت POJOهای ساده (Plain
اشیاء قدیمی جاوا)، فایل های MyBatis XML Mapper یا حتی اشیاء
کپسوله‌سازی تماس‌های خام JDBC یا درخواست‌های Hibernate. از یک میکرو
دیدگاه پلت فرم نرم، صفحه نمایش مشتری می تواند یک ASP (فعال) باشد
صفحات سرور) ماژول با استفاده از فریم ورک دات نت برای دسترسی به C# mod-
در لایه کسب و کار، با مشتری و دسترسی به داده های سفارش
ماژول های پیاده سازی شده به عنوان ADO (ActiveX Data Objects).
```

```
صفحه نمایش مشتری مسئول پذیرش درخواست و عدم پذیرش است.
پخش اطلاعات مشتری نمی داند کجا داده است
این است که چگونه بازیابی می شود، یا اینکه چند جدول پایگاه داده باید پرس و جو باشد
برای بدست آوردن داده ها هنگامی که صفحه مشتری درخواستی برای دریافت دریافت می کند
اطلاعات مشتری برای یک فرد خاص، سپس آن را فوروارد می کند
آن درخواست بر روی ماژول نمایندگی مشتری. این ماژول است
مسئول دانستن اینکه کدام ماژول ها در لایه تجاری می توانند
آن درخواست و همچنین نحوه رسیدن به آن ماژول و چه چیزی را پردازش کنید
داده های مورد نیاز (قرارداد). شی مشتری در لایه کسب و کار
مسئول جمع آوری تمام اطلاعات مورد نیاز است
درخواست تجاری (در این مورد برای دریافت اطلاعات مشتری). این
ماژول به ماژول dao مشتری (شیء دسترسی به داده) در آن فراخوانی می کند
لایه پایداری برای دریافت اطلاعات مشتری و همچنین سفارش دائو
ماژول برای دریافت اطلاعات سفارش این ماژول ها به نوبه خود اجرا می شوند
دستورات SQL برای بازیابی داده های مربوطه و ارسال آن ها
تا شیء مشتری در لایه تجاری. یک بار مشتری
شی داده ها را دریافت می کند، داده ها را جمع می کند و آن اطلاعات را ارسال می کند.
به نماینده مشتری بازگردانده می شود، که سپس آن را ارسال می کند
داده ها به صفحه مشتری برای ارائه به کاربر
```

```
برای نشان دادن نحوه عملکرد معماری لایه ای، یک درخواست را در نظر بگیرید
از یک کاربر تجاری برای بازیابی اطلاعات مشتری برای یک بخش خاص
فردی که در شکل 1-4 نشان داده شده است. فلش های سیاه نشان می دهد
درخواست برای بازیابی مشتری به پایگاه داده سرازیر می شود
داده‌ها و فلش‌های قرمز نشان می‌دهند که پاسخ به سمت بالا برمی‌گردد
صفحه نمایش برای نمایش داده ها در این مثال، اطلاعات مشتری
این شامل داده‌های مشتری و داده‌های سفارش (سفارش‌هایی است که توسط
مشتری).
```
